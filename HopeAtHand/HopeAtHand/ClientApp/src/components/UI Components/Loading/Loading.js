import React from 'react'

import {Typography} from '@material-ui/core'

import Filler from '../../HOC/Filler'

const Loading = (props) => {
    return <Typography variant='headline' align='center'>Loading</Typography>
}

export default Loading