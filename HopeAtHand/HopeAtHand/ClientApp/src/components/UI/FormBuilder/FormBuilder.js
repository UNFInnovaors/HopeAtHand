import React from 'react'
import { TextField } from '@material-ui/core';

export const mapJsonToForm = (jsonObject) => {
    console.log(jsonObject)
   
    switch(el.input){
        case("input") :
        return <TextField label={el.label }></TextField>
    }
    
}